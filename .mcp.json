{
  "mcpServers": {

    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://ux_admin:ux_dev_pass_2024@localhost:5436/universidadx"
      ],
      "description": "Acceso directo a PostgreSQL — consultas, inspección de esquema, análisis de datos"
    },

    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Documentación actualizada de Next.js, React, TypeScript, Kotlin, PostgreSQL, Docker y más"
    },

    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Grafo de conocimiento persistente — decisiones de arquitectura, entidades del proyecto, relaciones"
    },

    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "D:\\UniversidadX"
      ],
      "description": "Operaciones avanzadas sobre el árbol de archivos del proyecto"
    }

  }
}
